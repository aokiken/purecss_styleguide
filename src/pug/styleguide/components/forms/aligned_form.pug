form.pure-form.pure-form-aligned
  fieldset
    .pure-control-group
      label(for="name") Username
      input#name(type="text" placeholder="Username")
    .pure-control-group
      label(for="password") Password
      input#password(type="password" placeholder="Password")
    .pure-control-group
      label(for="email") Email Address
      input#email(type="email" placeholder="Email Address")
    .pure-control-group
      label(for="foo") Supercalifragilistic Label
      input#foo(type="text" placeholder="Enter something here...")
    .pure-controls
      label.pure-checkbox(for="cb")
        input#cb(type="checkbox")
        | I've read the terms and conditions
      button.pure-button.pure-button-primary(type="submit") Submit